[build]
    command = "python generate_blog_meta.py && zensical build --strict"
    publish = "site"
    environment = { PYTHON_VERSION = "3" }

[context.production.environment]
    SITE_URL = "https://tempered.works"