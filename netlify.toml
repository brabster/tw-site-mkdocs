[build]
    command = "python generate_blog_meta.py --step pre && zensical build --strict && python generate_blog_meta.py --step post"
    publish = "site"
    environment = { PYTHON_VERSION = "3" }

[context.production.environment]
    SITE_URL = "https://tempered.works"